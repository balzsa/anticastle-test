This is an unexpected exploit I found while messing with the game files. It helps dump unprotected variables but with their protected names, which is going to speed up Castleforge's progress. Confirmed working on 1.50.14.

Requires root, GameGuardian or another memory editor, but you can also binary patch for better results, with a much faster rate.

You need lck.root to use this exploit. However, the methods will not be given out publicly, so do not ask for them. I am perfectly fine with you discovering it by yourself. It probably won't do much for the average Joe, though.
